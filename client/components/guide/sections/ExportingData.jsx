import React from 'react';

import Anchor from '../Anchor';

export default () => (
  <div className="guide-section">
    <h2 className="guide-section-header">
      <Anchor id="exporting-data">Exporting Data</Anchor>
    </h2>

    <p>To export your data in CSV format, just click the export button at the
    top right of the Logs panel on the Dashboard.</p>
  </div>
);
